import React from 'react';

const ProjectInviteForm = () => {
    return (
        <form action="">
            <input type="text" />
            <button>Invite</button>
        </form>
    );
};

export default ProjectInviteForm;